# Server-only values. Do not prefix these with NEXT_PUBLIC_ because they must never be exposed to the browser bundle.
CSE_BACKEND_API_URL=http://localhost:5000
CSE_IMPORT_INTERNAL_SECRET=replace-with-your-backend-cse-import-secret

# Safe browser-visible label only.
NEXT_PUBLIC_MEGA_PANEL_NAME=CSE Mega Control Panel
