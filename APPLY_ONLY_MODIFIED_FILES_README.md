# Apply-only modified files package

This ZIP contains only files that are added or modified compared with the original uploaded project.
Copy/merge these files into the root of your original `CSE-AI-Proje-main` project, preserving folder paths.

## Files to delete manually from the original project

The following stale historical browser-direction docs were deleted from the final complete package. Remove them manually if they exist in your original project:

- `docs/A_Z_BROWSER_DOWNLOAD_FETCHER_NOTES.md`
- `docs/PACKAGE_FILE_MANIFEST_BROWSER_AZ.txt`

## Notes

- This is not a full project ZIP.
- It intentionally excludes unchanged files.
- It also excludes generated cache/build files such as `tsconfig.tsbuildinfo`, `node_modules`, `dist`, and `__pycache__`.
